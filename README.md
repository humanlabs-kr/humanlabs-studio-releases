# Humanlabs Studio — Update Feed

Public Sparkle appcast + signed DMG releases for **Humanlabs Studio**.
Source code lives in the private `humanlabs-kr/humanlabs-studio` repo; this repo
only hosts the auto-update feed (`appcast.xml`) and release DMGs so Sparkle can
fetch them anonymously.
